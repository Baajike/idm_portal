name: idm_portal_flutter
description: IDM Portal Flutter App
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  mobile_scanner: ^5.1.1
  http: ^1.2.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1
  device_info_plus: ^9.1.1  # ← ADDED: For device ID/IMEI collection

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/CSALOGO.png
    - assets/Gerald_Aboagye.jpg